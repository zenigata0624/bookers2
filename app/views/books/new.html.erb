     <h1>投稿フォーム</h1>
     <%= form_with model: @book do |f| %>
   <h4>名前</h4>
      <%= f.text_field :book_name %>
    <h4>説明</h4>
      <%= f.text_area :caption %>
    <%= f.submit '投稿' %>
  <% end %>